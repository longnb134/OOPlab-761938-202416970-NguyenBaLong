Answer for Section 14:
Java provides a feature called varargs using the syntax: addDigitalVideoDisc(DigitalVideoDisc... dvdList).
I prefer using varargs over an array parameter. Varargs allows the method to accept either an array or a comma-separated list of individual objects directly. 
This makes method calls cleaner without requiring the user to explicitly create an array.